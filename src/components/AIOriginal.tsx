import { RiGeminiFill, RiOpenaiFill } from 'react-icons/ri'
import { SiClaude, SiCursor } from 'react-icons/si'
import { SectionTitle } from './SectionTitle'
const tools = [['ChatGPT', RiOpenaiFill, 'Ideación, análisis de requerimientos, documentación y apoyo para llevar una solución de la idea al código.'], ['Claude', SiClaude, 'Comprensión de contexto extenso, diseño técnico, refactorización y revisión de código existente.'], ['Gemini', RiGeminiFill, 'Contraste de alternativas, investigación y lectura de información visual o documental.'], ['Cursor', SiCursor, 'Edición asistida dentro del repositorio, cambios entre archivos y aceleración de tareas repetitivas.']] as const
export function AIOriginal() { return <section id="ia" className="original-section ai-original" data-sec="04"><SectionTitle eyebrow="HERRAMIENTAS" title="Asistentes de IA que manejo" note="CADA HERRAMIENTA EN EL PUNTO DEL FLUJO DONDE MÁS APORTA" /><p className="ai-lead" data-reveal>No delego el criterio: uso la IA para explorar, implementar y revisar con mayor velocidad, siempre con contexto del proyecto y validación humana.</p><div className="ai-tool-grid">{tools.map(([name, Icon, body], index) => <article data-reveal key={name}><div><span>0{index + 1}</span><Icon /></div><h3>{name}</h3><small>USO FRECUENTE</small><p>{body}</p></article>)}</div></section> }
